# C4-style overview — ux-motion

> **Diátaxis:** explanation · **Canonical:** `docs/internals/c4.md` · **Layer:** ux-motion
> Map: [../INDEX.md](../INDEX.md).

Grounded in [../01-ARCHITECTURE.md](../01-ARCHITECTURE.md) and
[../14-CHANNEL-COMPOSITOR.md](../14-CHANNEL-COMPOSITOR.md).
This layer owns presence / transition plans as data (IR v1).
It does **not** own product behavior or DOM construction.

## Context

```mermaid
flowchart TB
  Author[Author]
  subgraph this["ux-motion (this repo)"]
    Scene["scene() builder"]
    Plan["Plan IR v1"]
    Send["send.play → Result ops"]
  end
  Channel["ux-channel — never learns transition.*"]
  Behavior["ux-behavior — XOR morph vs scene.enter"]
  Dom["ux-dom — HTML trees"]
  Compose["ux-compose — App.use_motion"]
  Author --> Scene --> Plan --> Send
  Send -->|"MotionChannel peels transition.*"| Channel
  Behavior -->|"XOR on one Result"| Send
  Dom -.->|"does not construct DOM"| this
  Compose --> Scene
```

Module cake: [../01-ARCHITECTURE.md](../01-ARCHITECTURE.md).
Diagrams: [../12-DIAGRAMS.md](../12-DIAGRAMS.md).
